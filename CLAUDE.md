# Engram

Serverless AWS microservice providing persistent, semantically-searchable memory for Claude across all conversations and projects. Vectors generated by Amazon Bedrock Titan Embeddings v2, stored in S3 Vector Tables, accessed via MCP server over mTLS.

## Naming Conventions

All AWS resources use the `engram-` prefix.

## Python Conventions

- **Runtime:** Python 3.12, target `arm64` (Graviton2)
- **Typing:** Explicit type hints on all function signatures. No `Any` in security-critical paths (auth, crypto, IAM). Use `from __future__ import annotations` in every module.
- **Models:** Pydantic v2 `BaseModel` for all request/response schemas. Use `Literal` for enums, `model_validator` for cross-field validation.
- **Logging:** `logging` stdlib with structured JSON output via a custom formatter for Lambda. Log tool name, scope, latency. Never log request bodies, tokens, keys, or PII.
- **Formatting:** f-strings. No `.format()` or `%`.
- **Imports:** stdlib, blank line, third-party, blank line, local. isort enforced.
- **Safety:** No mutable default arguments. Assert invariants on embedding outputs (length = 1024) before returning. No global mutable state.
- **Dependencies (Lambda):** Only stdlib + boto3 (built-in) + pydantic. Bundle pydantic into the deployment zip. No other third-party packages in Lambda.
- **Dependencies (MCP server):** `mcp`, `httpx`, `boto3`, `pydantic`.

## Terraform Conventions

- **Provider:** `hashicorp/aws ~> 5.0`
- **Format:** `terraform fmt` enforced. Validated by `terraform validate` and `tflint`.
- **Backend:** S3 + DynamoDB, bootstrapped in Phase 0.
- **Naming:** `snake_case` for HCL identifiers, `kebab-case` or the patterns above for AWS resource names.
- **Tags:** All resources receive default tags via provider block:
  ```hcl
  default_tags {
    tags = {
      Project   = "engram"
      ManagedBy = "terraform"
    }
  }
  ```
- **Variables:** Always include `description` and `type`. Use `validation` blocks for account IDs and regions.
- **Outputs:** Always include `description`. Mark sensitive values with `sensitive = true`.
- **No hardcoded identifiers:** Derive account ID from `data.aws_caller_identity.current`. Derive region from `data.aws_region.current`. ARNs are constructed from these.
- **Module structure:** Each module has `main.tf`, `variables.tf`, `outputs.tf`. No `providers.tf` inside modules. Cross-module wiring happens in the root `main.tf`.
- **Conditionals:** Use `for_each` with a set, not `count`.
- **Secrets in state:** Use `aws_secretsmanager_secret` for the shell only. Actual secret values are populated out-of-band. Add `lifecycle { ignore_changes = [secret_string] }` on any `aws_secretsmanager_secret_version` resources to prevent Terraform from overwriting manually-set values.

## Security Rules

1. **No secrets in code or TF state.** Lambda env vars contain bucket names and model IDs, never credentials or keys.
2. **S3 bucket policy:** Explicit deny for requests from external AWS accounts (`aws:PrincipalAccount` condition). TLS enforced via separate `aws:SecureTransport` deny.
3. **IAM:** Scoped to exact actions and resource ARNs. Explicit deny on Bedrock admin/discovery APIs (`ListFoundationModels`, `GetFoundationModel`, `CreateModelCustomizationJob`).
4. **Network:** Lambda runs outside VPC, communicating with AWS services (Bedrock, S3 Vectors, Secrets Manager) over their public endpoints. Access controlled by IAM least-privilege and resource policies.
5. **mTLS:** API Gateway validates the client cert against a truststore containing only the leaf certificate of the specific ACM exportable client cert. Pinning the leaf cert means no other cert -- including others signed by the same CA -- is trusted. Direct Lambda invocations (scheduler, cert rotator) are controlled by IAM and Lambda resource policies.
6. **Encryption at rest:** S3 SSE-KMS (`aws/s3` managed key). Secrets Manager default encryption.
7. **Private key handling:** Secrets Manager for server-side. age encryption for on-disk hook key. MCP server holds key in process memory only, never writes to disk.
8. **Blast radius:** Lambda reserved concurrency = 10. API Gateway throttle = 100 burst / 50 steady.

## Testing

- **Framework:** pytest
- **Unit tests** (`tests/unit/`): moto for S3 operations, `unittest.mock` for Bedrock and S3 Vectors (no moto support). All run without AWS credentials.
- **MCP tests** (`tests/mcp/`): mock HTTP client. Test tool schemas and request/response mapping.
- **Integration tests** (`tests/integration/`): require deployed infrastructure. Marker-gated with `@pytest.mark.integration`. Run with `pytest tests/integration/ --run-integration`.
- **Test naming:** `test_{method}_{scenario}_{expected_outcome}`
- **Terraform:** `terraform validate` + `tflint` on every change.
- **Smoke test:** `MEMORY_API_URL=https://... python3 scripts/smoke_test.py` for end-to-end mTLS store/recall/search_related verification.